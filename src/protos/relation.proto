syntax = "proto3";
option optimize_for = LITE_RUNTIME;

package proto;

import "id.proto";

// Relation from tag_id -> { content_ids }
//
message Relation {
  ID            tag_id      = 1;
  repeated ID   content_ids = 2;
}
